

function App() {
  
  return(
    <div>
      
    </div>
  )
}

export default App
// Remove the project already running (to a clear page). delete public folder, assets folder, App.css, index.css. Remove imports of these deleted ones from App.jsx and main.jsx.